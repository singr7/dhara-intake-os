# M9 — Enterprise & integrations · S33–S36

Milestone exit: AWS migration done; HMS/EMR + Sheets adapters; FHIR mapping + ABDM sandbox; BYOM with harness gating; 500-session scale; SLA posture.

---

## S33 — AWS migration

**Required reading:** docs 10 §5–7 (target architecture, triggers, data ops), 03 (ADR-014), 09 §3 (residency).

**Tasks**
1. Terraform (`infra/terraform/`): VPC (ap-south-1), RDS Postgres Multi-AZ, ElastiCache Redis, S3 (+lifecycle rules matching retention clocks), ECS Fargate (api, worker, relay with ALB sticky sessions), CloudFront for runner assets, Secrets Manager, CloudWatch; LiveKit placement decision (EC2 self-host vs cloud) revisited with real M7 traffic data.
2. Migration runbook + execution: data migration (pg_dump/restore or DMS), S3 sync, DNS cutover with rollback window, per-tenant maintenance notice. Omen box becomes staging + local-model host.
3. CI/CD retarget: image push to ECR, ECS deploy job, migration step, smoke gates; staging (omen) unchanged pipeline.
4. Scale test to 500 concurrent platform-wide (doc 10 §6) with autoscaling policies tuned; cost model of AWS run-rate vs tenant revenue documented.
5. SLA foundations: multi-AZ verification, error budgets, status page (simple), on-call escalation doc.

**Acceptance**
- Production serves from AWS with zero data loss (integrity checker pre/post), pilots uninterrupted (or planned window honored); 500-session k6 run green; rollback path tested on staging before cutover.

---

## S34 — Integration adapters: HMS/EMR + Google Sheets + Radpretation

**Required reading:** docs 02 §3.8, 07 §6, 01 §5 (adapters as moat).

**Tasks**
1. Adapter SDK pattern: export-target kinds become a plugin registry with a documented contract (transform structured output → target payload; delivery; status mapping) so per-pilot HMS adapters are cheap to add.
2. Google Sheets sink: service-account append to tenant-configured sheet (field mapping UI: output fields → columns); the NGO/survey favorite.
3. First HMS/EMR adapter: pick the pilot sites' actual system (likely bespoke/legacy — often "HL7-ish CSV/API"); implement against its reality; document the generic HL7v2 ORU/ADT sketch for the next one.
4. **Radpretation integration**: radiology-pack output → Radpretation study record association (patientRef/accession matching, history summary attached to the study for the radiologist) — the compounding play from doc 01 §3.
5. Integration health: per-target delivery dashboards, replay tools (re-export with idempotency), payload version pinning per target.

**Acceptance**
- A reviewed radiology intake appears attached to a Radpretation study; camp results stream into a Google Sheet live; adapter contract doc allows a new sink in <1 session.

---

## S35 — FHIR mapping + ABDM sandbox

**Required reading:** docs 02 §3.8, 09 §3, 01 §5 #9 (design-ready commitment).

**Tasks**
1. FHIR R4 mapping layer: structured output → FHIR resources (Patient [minimal], Encounter, QuestionnaireResponse [the natural fit for intake], Observation for discrete flags, DocumentReference for PDFs/uploads); export target kind `fhir` posting to a FHIR endpoint; mapping validated against a public FHIR validator.
2. Represent workflows as FHIR Questionnaire (lossy, one-way) for interoperability credibility; document mapping gaps.
3. ABDM sandbox integration: register in sandbox, implement ABHA-number capture (optional workflow field with verification via sandbox APIs), Consent Manager flow study → implement the HIP-side data-push happy path in sandbox; document the production-readiness gap list (do NOT go production ABDM yet — per doc 01 §10).
4. Compliance artifact pack for enterprise sales: DPDP posture doc, data-flow diagrams, retention schedule, audit sample — generated/maintained in `docs/compliance/`.

**Acceptance**
- QuestionnaireResponse for an OPD intake validates clean; sandbox ABHA verification round-trip works on staging; compliance pack reviewed by founder (and counsel if available — note in HANDOFF).

---

## S36 — BYOM + enterprise hardening

**Required reading:** docs 08 §8, 12 §5 (BYOM gate), 02 §3.4/§3.9, 01 §6 (enterprise pricing).

**Tasks**
1. BYOM: tenant-scoped `provider_configs` UI — register endpoint/keys for STT/LLM/TTS conforming to a published adapter HTTP contract (`docs/byom-contract.md`); connectivity + latency probe; **harness gate**: enablement requires a passing stored run of the tenant's packs' suites against the candidate config (server-enforced, from S31 machinery).
2. Enterprise tenancy options: SSO (SAML/OIDC) for consoles; IP allowlists; per-tenant S3 bucket option; the dedicated-DB tenant path designed (documented, provisioned via Terraform variable) — build only if a signed deal needs it (ADR-011).
3. API keys for integrators: scoped service tokens (read sessions, create sessions, webhooks admin) with rate limits + audit; public API docs site generated from OpenAPI.
4. Billing foundations: usage aggregation → invoice line items (completed reviewed intakes by pack/mode, platform fee, telephony passthrough) exportable to accounting; per doc 01 §6 pricing metric.
5. Platform ops maturity: tenant lifecycle (trial → active → suspended), quota defaults, abuse guards.
6. Full-platform demo recording + roadmap review: write `docs/roadmap/M10-planning.md` seeded from doc 11 M10 line, informed by three pilots' data.

**Acceptance**
- A fake "customer model" (our local STT behind the BYOM contract) onboarded end-to-end through the gate; an integrator API key drives session creation + webhook consumption in a sample script; invoice export matches analytics for a test month; M9 demo recorded and HANDOFF closes the sessioned roadmap.
